***کار با فایل****
*در این برنامه کاربر با وارد کردن نام کارمندان خود اقدام به ذخیره این اطلاعات میکند*

**کار بر ابتدا تعداد کارمندان خود را وارد میکند**
number=int(input("Enter your employe number: "))
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

**سپس با استفاده از حلقه ها تعداد پرسش وارد کردن نام کارمند به نمایش در می آید*
for i in range(number):
    name=input("Enter your employe name: ")
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
**سپس فایل مد نظر را با استفاده از متد w (write) باز میکنیم*

employ_file=open("test1.txt","w")
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
**نکته : برای جلو گیری از به هم چسپاندن نام کارمندان از n\ استفاده میکنیم 
employ_file.write("\n")
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

**سپس اقدام به ذخیره نام های وارد شده توسط کاربر در فایل میکنیم*
terminal_write=employ_file.write(name)
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
**سپس اقدام به چاپ n\ برای انتقال به خط جدید میکنیم*

employ_file.write("\n")
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
**سپس برای اعمال تغییرات فایل را میبندیم*
employ_file.close()
